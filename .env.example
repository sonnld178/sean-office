# SeanOffice — environment variables

# Server mode storage (S4) — Backblaze B2
# B2_KEY_ID=
# B2_APPLICATION_KEY=
# B2_BUCKET=
# B2_ENDPOINT=

# AI — Direct free keys (Gemini → Groq fallback)
# Vercel AI Gateway ($5 unlock, optional — comment nếu chỉ dùng demo free)
# AI_GATEWAY_API_KEY=
# Direct free keys (Groq No-limit, Gemini 1k/ngày)
# GEMINI_API_KEY=AIza...
# GROQ_API_KEY=gsk_...
# Optional paid fallback (not required):
# DEEPSEEK_API_KEY=
# HF_TOKEN=  # cho flux-schnell image gen nếu cần
# Legacy (deprecated): OPENROUTER_API_KEY=

# Turnstile (S6 rate limit)
# TURNSTILE_SECRET_KEY=
# NEXT_PUBLIC_TURNSTILE_SITE_KEY=

# Gmail OAuth (S7, optional)
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=

# Supabase project seanoffice (AI job queue ai_jobs — required for AI Fix queue)
# NEXT_PUBLIC_SUPABASE_URL=https://wkhdfujxlniurcixbgkm.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=  # server-only, worker bypass RLS

# RAG V1 — pgvector (file upload only, no website crawl)
# Migration: supabase/migrations/20260908_rag.sql enables extension vector (768-dim for Gemini text-embedding-004)
# Tables: company_memories + memory_chunks, cv_documents + cv_chunks; RLS enabled (service_role bypasses), ivfflat cosine index
# Functions: match_memory_chunks(query_embedding vector(768), match_count int, filter_user_key text), match_cv_chunks(...)
# Required for RAG: GEMINI_API_KEY (embeddings text-embedding-004) + NEXT_PUBLIC_SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY
# Embed dims: 768 — if changing model, update vector(768) in migration + search.ts
# Apply migration: orchestrator runs supabase db push (do NOT run supabase CLI locally for this task)
